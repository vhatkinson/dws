while(min===""||isNaN(min)){
    if(min===""){
        min=prompt("what's the minimum value? \nPlease do not leave blank.");
    }else{
        min=prompt("what's the minimum value? \nPlease only type in numbers.");
    }
}